package org.example;

public class HQL {

    // HQL --> Hibernate Query Language
    // It is a language which is derived from the SQL
    // In SQL, we'll work with Table name, column name but in HQL you have to work with Entity name(which is the replacement of TableName) and field name(which is the replacement
    // of the column name

    //Eg : In SQL -- > SELECT * FROM STUDENT;
    // But in HQL -- > FROM STUDENT;
}
